# Copy this file to keystore.properties (which is git-ignored) to sign local builds
# with a persistent key instead of the ephemeral debug keystore.
storeFile=/absolute/path/to/release.jks
storePassword=changeit
keyAlias=cuutruyen
keyPassword=changeit
